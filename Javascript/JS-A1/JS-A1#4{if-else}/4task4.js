// Task 4
let m= prompt('Enter the value of m');
let n= prompt('Enter the value of n');
if(m>n)
{	
	console.log(m + ' is larger then ' + n);
}4
else if(m==n)
	{
		console.log(m + ' & ' +n +' are equal');
	}
else
	{
	console.log(n + ' is Smaller than ' +m);
	}